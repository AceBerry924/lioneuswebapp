import React from 'react'
import { StoreListButton } from '../../assets/StyledComponent';

const ListButton = () => {
    return (
        <StoreListButton>
            <button><span>Add Product</span>
            </button>
        </StoreListButton>
    );
}

export default ListButton;